// This standalone router is not used inside the main app flow anymore.
// Kept here for reference if Hackathon needs to run standalone.
export default function HackathonStandaloneApp() {
  return null;
}
